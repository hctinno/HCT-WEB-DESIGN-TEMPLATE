# HCT 디자인 시스템

관리도구·대시보드·분석 화면을 위한 디자인 시스템 요약입니다. 지라·노션·슬랙·리니어를
참고해 만든 **고밀도 관리도구용** 시스템이며, 색상·타이포그래피·간격·레이아웃이 전부
토큰(코드)으로 정의돼 있어 그대로 가져다 쓸 수 있습니다.

> 이 문서는 "무엇이 있고 왜 이렇게 만들었는가"를 요약합니다. 컴포넌트 API·데이터 모델
> 규칙처럼 코드로 작업할 때 필요한 세부 규칙은 [`AGENTS.md`](./AGENTS.md) 를 보세요.

---

## 1. 원칙

1. **시맨틱 토큰만 쓴다.** `bg-blue-500` 같은 Tailwind 기본 팔레트는 이 시스템에서
   제거되어 있습니다. `bg-bg-surface`, `text-fg-primary` 같은 의미 기반 토큰만 존재하고,
   토큰 하나가 라이트·다크 모드 값을 동시에 가지고 있어 `dark:` 변형을 따로 쓸 필요가
   없습니다.
2. **상태는 색을 고르지 않는다.** "진행중"이 화면마다 다른 색이면 사용자는 색으로
   상태를 판단하는 법을 배우지 못합니다. 색은 필드 스키마에서 워크플로 상태 이름으로
   한 번만 정해지고, 모든 화면이 같은 색을 씁니다.
3. **관리도구는 하루 종일 보는 화면이다.** 본문 14px, 4px 배수 간격만 허용하는
   고밀도 스케일을 씁니다. 일반 웹사이트(16px)보다 조밀하지만, 그만큼 한 화면에
   더 많은 정보가 들어가 스크롤과 맥락 전환이 줄어듭니다.
4. **브랜드 교체는 값 하나로 끝난다.** 강조색 스케일과 폰트 변수만 바꾸면 시스템
   전체 색이 바뀝니다. 실제로 이 시스템은 6종의 완성된 팔레트를 제공합니다.

---

## 2. 색상

### 배경 — 4단 표면 체계

| 토큰 | 용도 |
|---|---|
| `bg-canvas` | 페이지 최하단 배경 |
| `bg-surface` | 카드·패널·테이블 본문 |
| `bg-raised` | 드롭다운·모달·팝오버 (떠 있는 것) |
| `bg-sunken` | 테이블 헤더·입력 내부·인셋 영역 |
| `bg-hover` / `bg-active` | 호버 · 눌림·선택 상태 |
| `bg-sidebar` | 사이드바 배경 (팔레트에 따라 본문과 밝기가 다를 수 있음) |

### 텍스트 — 4단 위계

`text-primary`(본문) → `text-secondary`(보조) → `text-tertiary`(메타) →
`text-disabled`(비활성). 한 화면에서 3단계를 넘겨 섞지 않는 걸 권장합니다 —
위계가 많아지면 위계가 사라집니다.

### 테두리

`border-subtle`(표 행 구분) · `border-default`(카드·입력) · `border-strong`(강한 구획)

### 상태 색상 — 6개 색조로 고정

| status | 색조 | 기본 라벨 | 의미 |
|---|---|---|---|
| `todo` | 회색 | 대기 | 아직 시작하지 않음 |
| `inProgress` | 파랑 | 진행중 | 처리 중 |
| `inReview` | 보라 | 검토중 | 확인 대기 |
| `blocked` | 빨강 | 차단됨 | 외부 요인으로 멈춤 |
| `done` | 초록 | 완료 | 정상 종료 |
| `failed` | 빨강 | 실패 | 오류로 종료 |
| `pending` | 주황 | 보류 | 조건 대기 |
| `active` / `inactive` | 초록 / 회색 | 활성 / 비활성 | 사용 중 / 사용 안 함 |

새 상태를 추가할 땐 임의 색을 만들지 않고 이 6개 색조 중 하나에 매핑합니다.
뱃지는 색만으로 의미를 전달하지 않도록 항상 텍스트 라벨과 함께 표시됩니다
(색각 이상 대응). 자세한 배경은 [`docs/status-colors.md`](./docs/status-colors.md).

### 브랜드 팔레트 — 6종

강조색과 사이드바 밝기만 바꿔 완전히 다른 느낌을 낼 수 있습니다. 시맨틱 토큰
레이어는 그대로 두고 `tokens/palettes.json` 의 값만 교체하는 구조입니다.

| 팔레트 | 성격 | 강조색(600) | 사이드바 |
|---|---|---|---|
| `hct` **(기본값)** | 로고색 강조 · 짙은 남색 사이드바 | `#2F4A9C` | 어두움 (`#17264D`) |
| `navy` | 짙은 남색 사이드바 · 흰 콘텐츠 | `#1B5AD6` | 어두움 (`#12253F`) |
| `arctic` | 전체 화이트 · 파랑 강조 | `#1B5AD6` | 밝음 (본문과 동일) |
| `graphite` | 무채색 강조 · 따뜻한 중성색 | `#33302A` | 밝음 (본문과 동일) |
| `plum` | 어두운 자두색 사이드바 · 슬랙 계열 구성 | `#8B2A6E` | 어두움 (`#33143A`) |
| `indigo` | 채도를 낮춘 남보라 · 중립 회색 | `#42449E` | 밝음 (본문과 동일) |

`hct`·`navy`·`plum` 은 **라이트 테마에서도 사이드바만 어둡게** 유지됩니다 —
탐색 영역(사이드바)과 작업 영역(본문)을 밝기로 갈라놓아, "지금 어디에 있는가"가
항상 배경으로 깔리고 콘텐츠는 밝아 데이터가 가장 진하게 남는 구성입니다. 노션·슬랙이
쓰는 것과 같은 구성입니다.

---

## 3. 로고

**대표 로고는 예외 없이 씁니다.** 실제 화면(AppShell 을 쓰는 모든 페이지)은
`SidebarBrand` 가 사이드바 상단에 로고를 강제로 배치합니다. 사이드바가 없는
결과물(랜딩 페이지, 비교 mockup 등)도 자체 워드마크나 플레이스홀더로 대신하지
않고 같은 로고 파일을 씁니다.

- 밝은 면 / 어두운 면용 **두 벌**(`hct-logo-color.png`, `hct-logo-white.png`)이
  있습니다 — 로고 원색은 어두운 배경에서 대비가 거의 없기 때문입니다.
- `on="light" | "dark" | "auto"` 는 **테마가 아니라 로고가 놓이는 면의 밝기**입니다.
  `hct`·`navy`·`plum` 팔레트는 라이트 테마에서도 사이드바가 어두우므로, 사이드바
  안에서는 항상 `on="dark"` 를 명시합니다.
- 로고 비율은 고정 — `height` 만 지정하고 늘리거나 찌그러뜨리지 않습니다.
- 폭이 좁은 자리(레일, 접힌 사이드바)에는 로고 대신 정사각 `LogoMark`를 씁니다 —
  타이핑한 글자가 아니라 **로고 자체에서 오려낸 "H"** 입니다. 로고의 기울어진
  획 스타일과 다른 서체로 된 글자를 넣으면 같은 브랜드로 안 읽힙니다.

---

## 4. 타이포그래피 — 고밀도 스케일

본문 기준 14px. 일반 웹(16px)보다 한 단계 조밀한, 지라·리니어·슬랙·깃허브가
공통으로 쓰는 크기입니다.

| 토큰 | 크기 / 행간 | 용도 |
|---|---|---|
| `micro` | 11 / 14px | 타임스탬프, 사이드바 그룹 라벨 |
| `xs` | 12 / 16px | 뱃지, 캡션, 입력 라벨 |
| `sm` | 13 / 18px | 밀집 테이블 셀, 사이드바 항목 |
| `base` | 14 / 20px | **기본 본문**, 버튼, 입력 |
| `md` | 16 / 24px | 섹션 제목 |
| `lg` | 20 / 28px | 페이지 제목 |
| `xl` | 24 / 32px | 화면 제목 |
| `metric` | 28 / 34px | KPI 수치 |
| `metric-lg` | 34 / 40px | 강조 KPI 수치 |

한 화면에서 4단계를 넘게 쓰지 않는 걸 권장합니다. 서체는 `--font-sans`
(Pretendard Variable 기반) / `--font-mono`(JetBrains Mono) 두 변수만 존재하고,
브랜드 교체 시 이 값만 바꾸면 전체 시스템 서체가 바뀝니다.

---

## 5. 간격 — 4px 그리드

허용값: `0 · 2 · 4 · 6 · 8 · 10 · 12 · 16 · 20 · 24 · 32 · 40 · 48 · 64 · 80 · 96`(px)

Tailwind 의 spacing 스케일 자체를 이 값들로 제한해뒀기 때문에, 스케일 밖 값
(`p-[13px]` 같은 임의 값)은 CSS 가 생성되지 않고 조용히 무시됩니다.

실무 기준:

| 용도 | 값 |
|---|---|
| 관련 요소 사이 | `8px` |
| 카드 내부 여백 | `16px` |
| 카드 사이 | `12px` |
| 섹션 사이 | `20~24px` |

모서리는 `sm 3px · md/기본 6px · lg 8px · xl 12px · full 9999px` 5단계,
그림자는 `sm · md · lg · overlay` 4단계로 고정돼 있습니다.

---

## 6. 레이아웃 — 앱 셸

모든 화면은 고정된 셸 구조 안에 들어갑니다. 노션·지라·슬랙이 화면을 옮겨 다녀도
탐색 위치와 조작 방법이 그대로 남는 것과 같은 이유입니다.

```
┌─────────────┬──────────────────────────────┬───────────────┐
│             │  Topbar (브레드크럼 + 전역 기능)      │               │
│  Sidebar    ├──────────────────────────────┤  RightPanel   │
│  (탐색)      │  Main                         │  (선택 항목    │
│             │   PageHeader(제목 + 액션)       │   상세, 선택   │
│             │   페이지 내용                    │   시에만)      │
└─────────────┴──────────────────────────────┴───────────────┘
```

| 영역 | 역할 | 넣지 말아야 할 것 |
|---|---|---|
| 사이드바 | 어디로 갈 수 있는가 | 페이지별 액션 버튼 |
| 상단바 | 지금 어디에 있는가 + 전역 기능 | 페이지 제목(PageHeader 와 중복) |
| 메인 | 지금 보는 내용 | 자체 헤더 · 자체 좌우 여백 |
| 우측 패널 | 선택한 항목의 상세 | 주 작업 흐름 |

**사이드바는 본문과 밝기가 다를 수 있습니다.** `hct`·`navy`·`plum` 팔레트에서는
사이드바만 어두워, 본문 토큰(`text-fg-primary` 등)을 사이드바 안에서 쓰면 글자가
배경에 묻혀 사라집니다. 사이드바 안에서는 `sidebar-*` 전용 토큰
(`bg-sidebar-bg`, `text-sidebar-fg`, `bg-sidebar-active-bg` 등)만 씁니다.

### 오버레이 선택 기준

| 상황 | 사용 | 이유 |
|---|---|---|
| 삭제 확인, 짧은 입력 | `ConfirmDialog` / `Modal` | 집중이 필요하고 되돌리기 어려움 |
| 상세 보기 · 편집 | `Drawer` | 목록 맥락 유지 |
| 상시 상세 패널 | `RightPanel` | 목록 ↔ 상세를 계속 오감 |

### 반응형

- **1024px 이상**: 사이드바 고정, 우측 패널 고정
- **1024px 미만**: 사이드바는 오버레이, 우측 패널은 전체 화면

---

## 7. 화면 원형

레이아웃 원칙이 실제로 조합되는 두 가지 대표 화면입니다.

**목록 + 뷰 + 상세** — `PageHeader` → (조건부 `Banner`) → `ViewTabs`(저장된 뷰) →
`QueryBar`(검색·필터·질의문) → `GridToolbar`(표/보드 전환·그룹핑·밀도) →
`DataGrid`/`BoardView` → 선택 시 `RightPanel` 상세. 이 모든 것이 하나의 질의
객체 위에서 동작합니다 — 뷰를 바꿔도, 그룹핑을 바꿔도 같은 질의 결과를 봅니다.

**대시보드** — `PageHeader`(기간 선택) → (조건부 `Banner`, 임계값을 넘긴 지표가
있을 때) → `MetricTile`(지표, 클릭 시 드릴다운) → `Widget`(분해, 차원 전환) →
"조치가 필요한 항목" 목록. 대시보드는 보기만 하는 화면이 아니라, 지표 →
분해 → 목록으로 좁혀 들어가는 경로로 끝나야 합니다.

---

## 8. 이 시스템이 참고한 것

지라(고밀도·상태 색), 노션(사이드바·문서형 상세), 슬랙(어두운 사이드바 · 밝은
본문 구성), 리니어(타이포 스케일)를 참고해 조합했습니다. 실제 코드에서
`tokens/tokens.json` 의 메타 정보에도 이 네 가지가 참고 대상으로 명시돼 있습니다.

---

## 더 보기

- [`AGENTS.md`](./AGENTS.md) — 이 시스템으로 실제 화면을 만들 때 지켜야 할 전체 규칙
  (컴포넌트 API, 데이터 모델, 접근성, 키보드 조작 등)
- [`docs/layout.md`](./docs/layout.md) · [`docs/density.md`](./docs/density.md) ·
  [`docs/status-colors.md`](./docs/status-colors.md) — 주제별 상세 문서
- [`tokens/tokens.json`](./tokens/tokens.json) · [`tokens/palettes.json`](./tokens/palettes.json) —
  실제 토큰 값 (이 문서의 원천)
