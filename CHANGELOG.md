# 변경 기록

소비 프로젝트가 **올려도 되는지 판단하려고** 읽는 문서입니다. 그래서 무엇이
바뀌었는지보다 **무엇이 깨지는지**를 먼저 씁니다.

버전 규칙:

| | 언제 | 소비 쪽에서 할 일 |
|---|---|---|
| **major** (2.0.0) | 컴포넌트 prop 이 없어지거나 이름이 바뀜, 토큰이 삭제됨, Tailwind 지원 범위가 줄어듦 | 코드를 고쳐야 합니다 |
| **minor** (1.1.0) | 화면·컴포넌트·토큰이 늘어남, 색이 조정됨 | 그냥 올려도 되지만 **화면을 한 번 보세요** — 색이나 간격이 달라졌을 수 있습니다 |
| **patch** (1.0.1) | 버그 수정, 문서 | 그냥 올리세요 |

디자인 시스템에서는 "색을 조금 다듬었다"가 소비 프로젝트 전체의 모습을
바꿉니다. 그래서 눈에 보이는 변화는 patch 로 내보내지 않습니다.

---

## v1.3.2

문서가 가리키는 이름이 실제로 있는지 아무도 확인하지 않고 있었습니다.

### `npm run audit:docs`

AGENTS.md 는 **다른 에이전트의 주 진입점**입니다. 거기 적힌 이름이 없으면
그 문서를 읽고 화면을 만드는 **모두가 같은 자리에서 막힙니다.** 그런데
문서와 코드가 맞는지는 사람이 읽어서 알아채기를 기대하고 있었습니다.

실제로 어긋난 적이 있습니다 — `foldToOther` 를 지운 뒤에도 AGENTS.md 는
"7번째 계열은 `foldToOther()` 로 묶습니다" 라고 안내하고 있었습니다.

이제 문서의 `<ComponentName />`, `` `functionName()` ``, `` `ComponentName` ``
이 배럴이나 lib 에서 실제로 나가는지 확인합니다. **확실한 것만** 봅니다 —
오탐이 하나라도 섞이면 사람이 검사를 안 믿게 되고, 그러면 진짜 실패도 함께
무시됩니다.

### 목록 화면이 휴지통을 자기 안에서 그리고 있었습니다

검사기가 처음 돌자마자 잡았습니다. AGENTS.md 의 행 액션 예시가
`<TrashIcon />` 을 쓰는데, 그건 `ListPage.jsx` 안의 **로컬 함수**였습니다.
예시를 복사한 사람은 없는 컴포넌트를 부르게 됩니다.

행 삭제는 목록 화면의 기본 동작인데 아이콘이 공용에 없어서, 화면마다
휴지통을 새로 그리고 선 굵기가 조금씩 달라지고 있었습니다. `NavIcons.Trash`
를 추가하고 목록 화면의 로컬 복사본을 지웠습니다.

→ 삭제 아이콘의 선 굵기가 다른 아이콘들과 같아집니다.

---

## v1.3.1

테스트를 안 덮은 모듈로 넓혔더니 **공유 링크가 조용히 다른 뜻이 되는 버그**가
나왔습니다.

### 값에 `:` `;` `,` 가 들어가면 링크가 깨졌습니다

```
"10:30" → f=title:eq:10%3A30 → 읽으면 "10"   (뒤가 잘림)
"a;b"   → 조건이 두 개로 쪼개짐
"a,b"   → 단일 값이 배열이 됨
```

인코딩은 맞았습니다. **푸는 순서**가 틀렸습니다. `URLSearchParams.get()` 이
쪼개기 전에 값을 통째로 풀어버려서, 값 안의 `%3A` 가 구조 구분자와 구별되지
않았습니다.

이제 **원본에서 꺼내고 → 쪼개고 → 조각마다 한 번 푼다** 순서를 지킵니다.
두 번 푸는 것도 함께 고쳤습니다 — 값이 `%3A` 라는 글자를 담고 있으면 그것까지
콜론으로 바뀌고 있었습니다.

콜론은 시각·비율·ID 에 흔합니다. "링크를 보냈는데 상대가 다른 데이터를 본다"
는 관리도구에서 가장 나쁜 종류의 버그입니다 — **틀렸다는 것조차 알 수 없습니다.**

→ `decodeQuery` 에는 **문자열을 넘기세요.** `URLSearchParams` 를 넘기면 값이
  이미 풀린 뒤라 복원할 수 없습니다. 그 경우 콘솔로 알려줍니다.

### 필터를 건드리면 다른 파라미터가 사라졌습니다

`useQuerySync` 가 주소의 검색 문자열을 **통째로 갈아치우고** 있었습니다.
그래서 질의와 무관한 파라미터 — 탭 상태(`?tab=activity`), 초대 토큰, 유입
경로 — 가 필터를 한 번 건드리는 순간 없어졌습니다.

미리보기에서 `?page=list` 로 들어가면 마운트 직후 주소가 비어버리는 것으로
확인했습니다. 오류가 안 나기 때문에 다음 새로고침에 가서야 "왜 다른 화면이
뜨지" 가 됩니다.

이제 **자기가 쓰는 파라미터(`q` `f` `m` `s` `g`)만** 갈아끼웁니다. 남의
파라미터는 값의 모양까지 그대로 둡니다 — `URLSearchParams` 로 다시 만들면
남의 값까지 재인코딩해서 링크가 바뀝니다.

`queryToUrl`(공유 링크)도 같은 문제가 있었고 함께 고쳤습니다.

### `cn` 의 "함께 갱신하세요" 를 기계가 지킵니다

`cn.js` 는 tailwind-merge 에 커스텀 스케일(`h-control-md`, `text-metric` …)을
등록합니다. 등록을 빠뜨리면 **아무 오류 없이** 두 클래스가 나란히 남고 승자를
CSS 순서가 정합니다 — 화면에서는 "className 이 가끔 안 먹는" 것처럼 보이고
원인을 찾기 매우 어렵습니다.

주석으로만 있던 규칙을 테스트로 옮겼습니다. 프리셋의 모든 커스텀 값에 대해
**실제로 충돌이 해소되는지** 양방향으로 확인하고, 실패하면 무엇을 등록해야
하는지 문구로 알려줍니다.

### 테스트 86개 → 132개

`queryUrl` 과 `cn` 이 새로 덮였습니다. 이제 순수 로직 모듈은 전부 테스트가
있습니다(React 훅과 DOM 의존 모듈 제외).

---

## v1.3.0

테스트가 하나도 없었습니다. 만들었더니 **화면이 죽는 버그 하나와 잘못된
표시 하나**가 바로 나왔습니다.

### 감사 로그에서 정렬 헤더를 누르면 화면이 죽었습니다

`toggleSort` 는 **질의 전체**를 받아 질의 전체를 돌려주는데, 감사 로그
화면만 `toggleSort(q.sort, key)` 로 부르고 있었습니다. 그러면 `query.sort` 가
배열의 `sort` **메서드**가 되어 `sort.find is not a function` 이라는,
원인을 짐작하기 어려운 오류가 납니다.

브라우저에서 확인했습니다 — 감사 로그의 열 머리글을 누르는 순간 죽습니다.
목록·대용량·사용자 화면은 맞게 부르고 있어서 멀쩡했습니다.

고쳤고, 같은 실수가 다시 나면 **무엇을 잘못 넘겼는지 말해주도록** 했습니다.
알아보기 힘든 오류는 고치는 데 오래 걸립니다.

### 값이 없는 날짜가 "1970.01.01" 로 표시됐습니다

`new Date(null)` 은 NaN 이 아니라 **에포크**입니다. NaN 검사만으로는
못 걸러냅니다. 그래서 한 번도 성공한 적 없는 작업의 "마지막 성공" 이
1970년으로 나왔습니다.

`formatValue` 를 거치는 표는 멀쩡했는데, `formatRelative`·`formatDate` 를
화면이 직접 부르는 자리가 12곳 넘게 있어서 거기로 샜습니다.

→ 값이 없는 날짜는 이제 빈 칸입니다. 진짜 타임스탬프 `0` 은 그대로 1970년으로
  표시됩니다 — 값이 없는 것과 0은 다른 뜻입니다.

### `npm test`

```bash
npm test    # 86개
```

의존성이 없습니다. Node 내장 `node:test` 를 그대로 씁니다 — 이 저장소의
검사 스크립트가 전부 순수 node 인 것과 같은 이유입니다.

**순수 로직만** 봅니다: 질의·필드·CSV·가져오기 검증·지표·차트 토큰.
화면은 브라우저와 `audit:a11y` 로 확인합니다. 로직 층을 따로 지키는 이유는
여기가 **스키마 하나에서 모든 화면이 파생되는 자리**이기 때문입니다 —
`applyQuery` 가 조용히 틀리면 표도 보드도 대시보드도 함께 틀립니다.

지금까지 이 저장소에서 나온 버그는 **전부 이 층에 있었고 전부 우연히
발견됐습니다.** 그래서 각 버그를 회귀 테스트로 남겼습니다:

| 회귀 테스트 | 원래 어떻게 발견했나 |
|---|---|
| 미래 날짜가 "방금" 으로 | 모니터링 앱을 만들어보다 |
| 오타 난 연산자가 필터를 조용히 해제 | 예시에서 `op:` 라고 잘못 씀 |
| `foldSeries` 의 기본값이 안 먹음 | 버리는 스크립트를 하나 써보다 |
| 정렬 헤더가 화면을 죽임 | **이번 테스트가 잡음** |
| 빈 날짜가 1970년 | **이번 테스트가 잡음** |

CI 에 들어갔습니다.

### 테스트 명령의 글로브는 따옴표로 감싸면 안 됩니다

`--test` 의 글로브 확장은 **Node 22 부터**입니다. 따옴표로 감싸서 넘기면
Node 20 은 그것을 파일 이름으로 읽고 `Could not find '.../test/*.test.mjs'`
로 죽습니다. 감싸지 않으면 셸이 먼저 파일 경로로 펼쳐서 어느 버전에서든
동작합니다.

로컬(Node 22)에서는 통과해서 못 보고 넘어갔고 **CI(Node 20)가 잡았습니다.**
CI 의 Node 를 올려서 이 신호를 없애지 않았습니다 — 소비 프로젝트가 어떤
Node 를 쓸지 모르고, 이 차이를 알려주는 건 지금 CI 뿐입니다.

### 로직 층이 번들 없이도 import 됩니다

`src/lib` 안의 상대 import 세 줄에 `.js` 확장자를 붙였습니다. 확장자 없는
상대 import 는 번들러를 거쳐야만 해석됩니다. 이제 순수 Node 로도 그대로
불러올 수 있습니다 — 테스트가 가능해진 이유이기도 합니다.

→ 소비 프로젝트에는 영향이 없습니다. 배포되는 것은 번들된 `dist` 입니다.

---

## v1.2.0

BI 디자인 시스템(Grafana · IBM Carbon · Cloudscape · Adobe Spectrum)의 색 규칙을
우리 코드에 들이대 보고 고친 판입니다. **재보니 세 군데가 무너져 있었습니다.**

### 선이 색으로만 구별되고 있었습니다

계열 색 6종은 명도를 맞추고 색상만 바꾼 팔레트입니다. 순위처럼 보이지 않게
하려는 정당한 선택인데, 그 대가로 **계열끼리 명도로는 구별되지 않습니다.**
재봤더니 **15쌍 전부 3:1 미만**이고 가장 가까운 쌍은 `1.01:1` — 사실상 같은
밝기입니다. 흑백 인쇄·색각 이상에서 여섯 선이 한 덩어리가 됩니다.

정작 AGENTS.md 에는 "색만으로 상태를 전달하지 마세요" 라고 적혀 있습니다.
프레즌스 점은 모양까지 다르게 하면서 차트에는 그 규칙을 적용하지 않았습니다.

이제 선마다 파선 패턴이 다릅니다(`SERIES_DASH`). **범례와 툴팁도 같은 선
모양**을 보여줍니다 — 선은 파선인데 범례가 사각형이면 둘을 맞출 수 없습니다.

→ 선 차트의 모습이 달라집니다. 계열이 하나면 예전처럼 실선입니다.

### `chart-2` 가 대비 기준에 미달이었습니다

`#D4720E` → `#CC6D0C`. 흰 배경에서 3.38:1 이라 Carbon 의 데이터 시각화 기준
3.5:1 에 못 미쳤습니다. WCAG 1.4.11 의 3:1 은 넘었지만 여유가 없었습니다.

→ 주황 계열(2번)이 아주 조금 진해집니다.

### "7번째는 기타로 묶습니다" — 묶는 코드가 없었습니다

`chartTokens.js` 주석이 약속했는데 실제로는 넘치는 계열을 **전부 같은
회색**으로 칠했습니다. 7·8·9번이 서로도 구별되지 않아서, 범례에는 세 항목이
따로 있는데 차트에는 똑같은 회색 선 세 개가 겹쳐 있었습니다.

`foldSeries()` 를 추가했습니다. **자르지 않고 합칩니다** — 조각을 다 더했는데
총계와 다르면 사용자는 그때부터 숫자를 믿지 않습니다. `BreakdownList` 가 이미
쓰고 있어서, 담당자처럼 값이 열 개 넘는 차원도 상위 5개 + '기타 N개' 로 나옵니다.
접지 않고 색보다 많은 계열을 넘기면 이제 콘솔이 알려줍니다.

→ 차원 값이 6개를 넘는 분해 위젯의 목록이 짧아집니다. 합계는 그대로입니다.

### 순차형 팔레트가 없었습니다

"많다/적다" 를 진하기로 보여줄 토큰이 아예 없어서 `MatrixTable` 은 숫자만
나열했습니다. 어느 칸이 뜨거운지 보려고 열두 칸을 다 읽어야 했습니다.

`--chart-seq-1`~`5` 를 추가하고 `<MatrixTable heat>` 을 켤 수 있게 했습니다.
숫자는 그대로 찍히므로 색을 못 봐도 잃는 것이 없습니다 — 그래서 검사 기준도
'칸 대 배경'이 아니라 **칸 위의 글자가 4.5:1 을 넘는가** 입니다. 0은 칠하지
않습니다(빈 칸과 '적은 값'은 다른 뜻입니다).

`ChartTable`(차트의 표 대체본)이 이제 `MatrixTable` 로 그려지면서 열 지도가
켜집니다. 표를 두 군데서 따로 짜던 것도 하나로 합쳐졌습니다. `formatValue` 는
그대로 받습니다 — prop 이 사라지면 major 인데, 이건 그럴 만한 변경이 아닙니다.

→ 대시보드에서 '표 보기' 를 누르면 숫자에 진하기가 생깁니다.

### 그리고 이 모든 걸 기계가 재게 했습니다

```bash
npm run audit:charts
```

차트의 선과 막대는 **글자가 아니라 그래픽 객체**입니다. 팔레트 검사는 글자
대비만 보고, axe-core 는 SVG 도형의 색을 아예 검사하지 않습니다. 즉 지금까지
차트 색은 **아무도 재지 않는 상태**였고, `tokens.css` 의 "대비·색각 검증을
통과한 값입니다" 라는 주석은 근거가 없었습니다.

이 검사는 파선이 실제로 적용되어 있는지도 확인합니다. 명도가 같은 팔레트는
파선 없이는 규격 위반이므로, 한쪽만 지우는 일을 기계가 막아야 합니다.
`tokens:build` 와 CI 에 들어갔습니다.

### 그 밖

- `audit:exports` 가 배럴의 주석을 export 이름으로 잘못 읽던 것을 고쳤습니다.
  검사기 때문에 주석을 지우게 되면 안 됩니다.

---

## v1.1.0

기능을 더한 판이 아닙니다. **재는 도구를 만들어서 돌려본 판**입니다.
지금까지는 "화면을 띄워서 눈으로 본다" 가 유일한 검증이었는데, 눈으로는
못 보는 종류의 문제가 남아 있었습니다.

### 카드 격자의 간격이 화면마다 달랐습니다

세어보니 화면 7곳이 격자를 직접 만들면서 간격이 `gap-2` · `gap-2.5` ·
`gap-3` 으로 갈라져 있었습니다. 2px 차이는 한 화면 안에서는 안 보입니다.
화면을 넘나들 때 "같은 제품이 아닌 것 같은" 느낌으로만 남습니다 —
정확히 이 저장소가 막으려고 존재하는 종류의 문제입니다.

정작 이러라고 만들어 둔 `StatGrid` 는 **한 번도 쓰이지 않았습니다.**

이제 격자는 둘뿐입니다:

- `<StatGrid columns={2|3|4}>` — 지표 타일·요약 카드 (sm 부터 2열)
- `<WidgetGrid columns={2|3}>` — 차트·위젯 (lg 전까지 1열)

나뉜 이유는 접히는 지점입니다. 축과 범례가 있는 차트를 640px 에서 반으로
자르면 눈금이 겹쳐 못 읽습니다. 화면에서 격자를 직접 만들면 이제
`lint:design` 이 실패시킵니다(`no-adhoc-card-grid`).

→ 대시보드 지표 타일의 간격이 10px 에서 12px 로 넓어집니다. 그 외 화면은
  그대로입니다.

### 대시보드 원형의 부품이 공개되어 있지 않았습니다

`MetricTile` · `Widget` · `BreakdownList` · `Sparkline` 이 배럴에 없었습니다.
그래서 이 패키지로 모니터링 화면을 만들어보니 지표 타일을 **직접 만들게
되었고**, 그 순간 임계값 색·델타 화살표·드릴다운 규칙이 원형과 갈라졌습니다.
공개 목록에서 빠진 것은 없는 것과 같습니다. 이제 배럴에서 가져올 수 있습니다.

### 로딩 상태를 넣을 방법이 없었습니다

문서는 "로딩·빈 상태·에러를 항상 함께 구현하라" 고 적어두었는데, 정작
`StatCard` 와 `Widget` 에는 로딩을 표현할 방법이 없었습니다. 그래서 다들
카드를 통째로 숨겼고, 다 불러온 순간 아래 내용이 통째로 밀렸습니다.

`loading` 을 켜면 자리를 지킨 채 스켈레톤이 됩니다. `Widget` 은 제목을
남기고 본문만 바뀝니다 — 격자가 다시 짜이지 않게.

### 테마가 한 번 번쩍였습니다

미리보기가 저장된 테마를 `useEffect` 에서 적용하고 있었습니다. effect 는
첫 페인트 **다음에** 돌기 때문에, 다크를 골라둔 사람에게 흰 화면이 한 번
비치고 어두워졌습니다. 저장된 값을 아는 시점이 렌더 전인데 렌더 뒤까지
기다릴 이유가 없습니다. 이제 렌더 전에 적용합니다.

→ 소비 프로젝트도 같습니다. 진입점 맨 위에서 `initTheme()` · `initPalette()`
  를 부르세요.

### 아무도 안 쓰는 공개 컴포넌트 8개를 정리했습니다

`npm run audit:exports` 를 만들어 세어보니 179개 중 8개가 정의만 있고 아무
데서도 쓰이지 않았습니다. 넷은 지웠습니다:

- `LegendItem` — `ChartLegend` 의 항목과 같은 것을 두 번 정의하고 있었습니다
- `foldToOther` — `{key, value}` 를 기대하는데 이 시스템은 `{key, count, share}`
  만 만듭니다. 아무 데도 맞지 않는 인터페이스였습니다
- `ButtonGroup` — `flex items-center gap-2` 한 줄이었습니다. 결정이 아닙니다
- `PanelSection` — 패널 내용의 구조는 `ObjectDetail` 이 이미 가집니다

넷은 실제로 쓰이게 했습니다(`StatGrid`, `SkeletonText`, `SkeletonStatCard`,
`initPalette`). 공개 목록에 아무도 안 쓰는 것이 남아 있으면 다음 에이전트의
선택지만 늘어나고, 통일성은 그만큼 약해집니다.

→ 지운 넷을 쓰고 있었다면 고쳐야 합니다. 다만 이 넷은 이 저장소 안에서도
  쓰인 적이 없어, 실제로 영향을 받는 코드는 없을 가능성이 높습니다.

### 설치 방법이 태그에서 `release/` 브랜치로 바뀝니다

```bash
npm i github:hctinno/HCT-WEB-DESIGN-TEMPLATE#release/v1.1.0
```

이 저장소는 에이전트가 원격 실행 환경에서 관리하는데, 그 환경은 브랜치
푸시는 허용하면서 **태그 푸시를 막습니다**(HTTP 403). 그래서 태그로 배포를
묶어두면 코드가 다 준비되고도 사람이 웹 UI 에서 릴리스를 만들어주기 전까지
아무도 설치할 수 없습니다. **v1.0.1 과 v1.0.2 가 실제로 그렇게 묻혔습니다** —
변경 기록에는 있는데 설치할 수는 없는 판이었습니다.

npm 의 `github:` 설치는 `#` 뒤에 태그·브랜치·커밋 SHA 를 모두 받습니다. 셋 다
똑같이 동작하니, **항상 만들 수 있는 것**을 쓰는 게 맞습니다. `release/vX.Y.Z`
브랜치는 한 번 만든 뒤 움직이지 않으므로 소비 쪽에서는 태그와 구별되지
않습니다. 더 확실하게 하려면 커밋 SHA 를 그대로 써도 됩니다.

→ 기존 `#v1.0.0` 설치는 그대로 동작합니다. 다음에 올릴 때 `#release/v1.1.0`
  으로 바꾸세요.

### 접근성을 처음으로 재봤습니다

`npm run audit:a11y` — axe-core 로 20개 화면 × 2개 폭 × 2테마, 80건을
WCAG 2.1 A/AA 로 검사합니다. **위반 0건**입니다.

지금까지 "접근성 — 협상 불가" 라고 적어두고 한 번도 측정한 적이 없었습니다.
문서에 적힌 약속을 기계가 확인하지 않으면 그건 약속이 아닙니다. CI 가
PR 마다 돌립니다.

0건이 되기까지 고친 것들 — 전부 화면을 봐서는 못 찾는 것들입니다:

- **사이드바 배지 대비 2.66:1.** 활성 항목은 배경이 강조색이라 그 위에
  `sidebar-subtle` 을 얹으면 숫자가 거의 안 보였습니다. 활성일 때는 활성
  글자색을 씁니다
- **`role="grid"` 가 `<table>` 이 아니라 스크롤 `<div>` 에** 붙어 있었습니다.
  화면낭독기가 표를 표로 읽지 못합니다
- **`<ul>` 안에 `<ul>`** — 저장된 뷰 목록이 목록을 중첩해 만들고 있었습니다
- **이름 없는 컨트롤** — `Progress` 막대와 `Dropzone` 의 파일 입력
- **11px 글자의 대비 4.44:1** — 기준(4.5)에 아슬아슬하게 못 미쳤습니다.
  작은 글자일수록 한 단계 진한 색이 필요합니다

---

## v1.0.2

좁은 화면(390px)에서 처음으로 렌더해보고 고친 것들입니다. 지금까지 1440px
에서만 확인했는데, 정작 제출 전 확인 목록에는 "1024px 미만" 항목을 적어두고
정작 해본 적이 없었습니다.

### PageHeader 가 좁은 화면에서 깨졌습니다

제목과 액션이 항상 가로로 나란히 있어, 액션이 폭을 다 가져가면 제목 영역이
0 에 가까워졌습니다. 제목은 `운영...` 으로 잘리고 설명은 한 어절씩 세로로
쪼개져 읽을 수 없었습니다.

이제 sm 미만에서는 제목과 액션을 세로로 쌓고, 제목은 두 줄까지 허용합니다.

### Stepper 가 모바일에서 무슨 단계인지 알 수 없었습니다

`1 파... 2 열... 3 확... 4 가져...` — 네 단계를 390px 에 나란히 넣을 방법은
없습니다. 이제 좁은 화면에서는 `1 / 4 단계 · 파일 올리기` 처럼 지금 어디인지만
온전한 문장으로 보여주고, 진행은 막대로 표시합니다.

### 그 밖

- 가져오기 화면의 파일 힌트가 `.csv · 최대 20MB · 최대 20MB` 로 겹쳤습니다.
  Dropzone 이 이미 형식과 용량을 적으므로 힌트에서 뺐습니다.

→ 세 가지 모두 좁은 화면에서만 달라집니다. 데스크톱 화면은 그대로입니다.

---

## v1.0.1

버그 수정 두 건. 둘 다 **실제로 모니터링 시스템을 하나 만들어보다** 발견했습니다 —
템플릿 안에서는 드러나지 않던 것들입니다.

### 미래 날짜가 전부 "방금" 으로 나왔습니다

`formatRelative` 가 과거만 가정하고 있었습니다. 교정 만료가 9일 남았는데
"방금" 이라고 쓰면 이미 만료된 것처럼 읽힙니다. 만료일·기한·다음 실행처럼
**미래를 가리키는 날짜는 관리도구에 흔합니다.**

이제 `9일 후`, `3시간 후` 처럼 나옵니다. 일주일이 넘으면 상대 시간이 감이
안 오므로 날짜를 그대로 보여줍니다(기존 동작과 같습니다).

→ 날짜 필드를 미래 값으로 쓰던 화면이 있었다면 표시가 바뀝니다. 과거 날짜는
  그대로입니다.

### 오타 난 연산자가 필터를 조용히 풀어버렸습니다

`applyQuery` 가 모르는 연산자를 만나면 조건을 통과시켰습니다. 그러면 화면에는
"상태 = 고장" 칩이 붙어 있는데 실제로는 **전체 목록**이 나옵니다. 사용자는
그게 걸러진 결과라고 믿고 일괄 작업을 겁니다.

이제 아무것도 통과시키지 않고 콘솔에 무엇이 잘못됐는지 적습니다. 조용히
틀린 것보다 시끄럽게 비어 있는 편이 낫습니다.

→ 지금까지 이 버그 덕분에 "우연히 동작하던" 질의가 있었다면 결과가 비어
  보입니다. 콘솔에 찍힌 연산자 이름을 고치세요.
  (스키마에서 사라진 필드를 가리키는 저장된 뷰는 예전처럼 조건만 무시합니다.)

---

## v1.0.0

첫 배포. 다른 프로젝트가 설치해서 쓸 수 있는 형태가 되었습니다.

### 들어 있는 것

- **화면 원형 16종** — 대시보드·목록·대용량·알림 인박스·가져오기 마법사·
  승인 대기·예약 작업·검색 결과·사용자와 권한·감사 로그·연동과 API·
  사용량과 청구·환경설정·내 계정·로그인/인증 흐름·빈 워크스페이스·오류 4종
- **컴포넌트** — 앱 셸, DataGrid(가상화·키보드·인라인 편집), 보드 뷰,
  질의 빌더, 객체 상세, 차트, 폼, 오버레이, 상태 표시
- **데이터 모델** — 필드 스키마 하나에서 표·보드·상세·필터·질의·대시보드가
  전부 파생됩니다
- **색 팔레트 6종** — 전부 WCAG 대비 검사를 통과해야 빌드됩니다
- **Tailwind 3.x·4.x 지원** — 토큰은 한 곳(`tailwind-preset.js`)에서 나오고
  v4 용 `@theme` 은 거기서 생성됩니다

### 통일성이 강제되는 방식

- Tailwind 기본 색 팔레트를 제거했습니다. `bg-blue-500` 은 **CSS 가
  생성되지 않습니다**(v3·v4 모두).
- 간격은 4px 배수만 존재합니다. `p-7`, `h-7` 도 CSS 가 생성되지 않습니다.
- `npm run lint:design` 이 13개 규칙으로 빌드를 실패시킵니다.
- CI 가 PR마다 규칙·빌드·생성 파일 최신 여부를 확인하고, 진짜 소비
  프로젝트를 v3·v4 양쪽으로 만들어 설치·빌드까지 검증합니다.

### 알아둘 것

- Tailwind v3 에서는 `tailwind.config.js` 의 `content` 에 `hctContent` 를
  반드시 펼쳐 넣어야 합니다. v3 는 프리셋의 `content` 를 병합하지 않아,
  빼먹으면 컴포넌트 클래스가 전부 purge 되고 **오류 없이** 스타일만 빠진
  화면이 나옵니다. 빼먹으면 `AppShell` 이 콘솔에 고칠 방법을 적어줍니다.
  (v4 는 `@source` 가 패키지 CSS 안에 있어 이 문제가 없습니다.)
- 팔레트·테마 설정은 하지 않아도 됩니다. 아무 속성도 붙이지 않으면 기본
  팔레트가 그대로 적용됩니다.
- 로고 원본이 264×86 래스터입니다. 화면에는 충분하지만 인쇄·대형 확대에는
  부족합니다. SVG 가 확보되면 `Logo.jsx` 만 고치면 됩니다.
