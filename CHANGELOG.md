# 변경 기록

소비 프로젝트가 **올려도 되는지 판단하려고** 읽는 문서입니다. 그래서 무엇이
바뀌었는지보다 **무엇이 깨지는지**를 먼저 씁니다.

버전 규칙:

| | 언제 | 소비 쪽에서 할 일 |
|---|---|---|
| **major** (2.0.0) | 컴포넌트 prop 이 없어지거나 이름이 바뀜, 토큰이 삭제됨, Tailwind 지원 범위가 줄어듦 | 코드를 고쳐야 합니다 |
| **minor** (1.1.0) | 화면·컴포넌트·토큰이 늘어남, 색이 조정됨 | 그냥 올려도 되지만 **화면을 한 번 보세요** — 색이나 간격이 달라졌을 수 있습니다 |
| **patch** (1.0.1) | 버그 수정, 문서 | 그냥 올리세요 |

디자인 시스템에서는 "색을 조금 다듬었다"가 소비 프로젝트 전체의 모습을
바꿉니다. 그래서 눈에 보이는 변화는 patch 로 내보내지 않습니다.

---

## v1.1.0

기능을 더한 판이 아닙니다. **재는 도구를 만들어서 돌려본 판**입니다.
지금까지는 "화면을 띄워서 눈으로 본다" 가 유일한 검증이었는데, 눈으로는
못 보는 종류의 문제가 남아 있었습니다.

### 카드 격자의 간격이 화면마다 달랐습니다

세어보니 화면 7곳이 격자를 직접 만들면서 간격이 `gap-2` · `gap-2.5` ·
`gap-3` 으로 갈라져 있었습니다. 2px 차이는 한 화면 안에서는 안 보입니다.
화면을 넘나들 때 "같은 제품이 아닌 것 같은" 느낌으로만 남습니다 —
정확히 이 저장소가 막으려고 존재하는 종류의 문제입니다.

정작 이러라고 만들어 둔 `StatGrid` 는 **한 번도 쓰이지 않았습니다.**

이제 격자는 둘뿐입니다:

- `<StatGrid columns={2|3|4}>` — 지표 타일·요약 카드 (sm 부터 2열)
- `<WidgetGrid columns={2|3}>` — 차트·위젯 (lg 전까지 1열)

나뉜 이유는 접히는 지점입니다. 축과 범례가 있는 차트를 640px 에서 반으로
자르면 눈금이 겹쳐 못 읽습니다. 화면에서 격자를 직접 만들면 이제
`lint:design` 이 실패시킵니다(`no-adhoc-card-grid`).

→ 대시보드 지표 타일의 간격이 10px 에서 12px 로 넓어집니다. 그 외 화면은
  그대로입니다.

### 대시보드 원형의 부품이 공개되어 있지 않았습니다

`MetricTile` · `Widget` · `BreakdownList` · `Sparkline` 이 배럴에 없었습니다.
그래서 이 패키지로 모니터링 화면을 만들어보니 지표 타일을 **직접 만들게
되었고**, 그 순간 임계값 색·델타 화살표·드릴다운 규칙이 원형과 갈라졌습니다.
공개 목록에서 빠진 것은 없는 것과 같습니다. 이제 배럴에서 가져올 수 있습니다.

### 로딩 상태를 넣을 방법이 없었습니다

문서는 "로딩·빈 상태·에러를 항상 함께 구현하라" 고 적어두었는데, 정작
`StatCard` 와 `Widget` 에는 로딩을 표현할 방법이 없었습니다. 그래서 다들
카드를 통째로 숨겼고, 다 불러온 순간 아래 내용이 통째로 밀렸습니다.

`loading` 을 켜면 자리를 지킨 채 스켈레톤이 됩니다. `Widget` 은 제목을
남기고 본문만 바뀝니다 — 격자가 다시 짜이지 않게.

### 테마가 한 번 번쩍였습니다

미리보기가 저장된 테마를 `useEffect` 에서 적용하고 있었습니다. effect 는
첫 페인트 **다음에** 돌기 때문에, 다크를 골라둔 사람에게 흰 화면이 한 번
비치고 어두워졌습니다. 저장된 값을 아는 시점이 렌더 전인데 렌더 뒤까지
기다릴 이유가 없습니다. 이제 렌더 전에 적용합니다.

→ 소비 프로젝트도 같습니다. 진입점 맨 위에서 `initTheme()` · `initPalette()`
  를 부르세요.

### 아무도 안 쓰는 공개 컴포넌트 8개를 정리했습니다

`npm run audit:exports` 를 만들어 세어보니 179개 중 8개가 정의만 있고 아무
데서도 쓰이지 않았습니다. 넷은 지웠습니다:

- `LegendItem` — `ChartLegend` 의 항목과 같은 것을 두 번 정의하고 있었습니다
- `foldToOther` — `{key, value}` 를 기대하는데 이 시스템은 `{key, count, share}`
  만 만듭니다. 아무 데도 맞지 않는 인터페이스였습니다
- `ButtonGroup` — `flex items-center gap-2` 한 줄이었습니다. 결정이 아닙니다
- `PanelSection` — 패널 내용의 구조는 `ObjectDetail` 이 이미 가집니다

넷은 실제로 쓰이게 했습니다(`StatGrid`, `SkeletonText`, `SkeletonStatCard`,
`initPalette`). 공개 목록에 아무도 안 쓰는 것이 남아 있으면 다음 에이전트의
선택지만 늘어나고, 통일성은 그만큼 약해집니다.

→ 지운 넷을 쓰고 있었다면 고쳐야 합니다. 다만 이 넷은 이 저장소 안에서도
  쓰인 적이 없어, 실제로 영향을 받는 코드는 없을 가능성이 높습니다.

### 설치 방법이 태그에서 `release/` 브랜치로 바뀝니다

```bash
npm i github:dytc880915-commits/hct-web-design-template#release/v1.1.0
```

이 저장소는 에이전트가 원격 실행 환경에서 관리하는데, 그 환경은 브랜치
푸시는 허용하면서 **태그 푸시를 막습니다**(HTTP 403). 그래서 태그로 배포를
묶어두면 코드가 다 준비되고도 사람이 웹 UI 에서 릴리스를 만들어주기 전까지
아무도 설치할 수 없습니다. **v1.0.1 과 v1.0.2 가 실제로 그렇게 묻혔습니다** —
변경 기록에는 있는데 설치할 수는 없는 판이었습니다.

npm 의 `github:` 설치는 `#` 뒤에 태그·브랜치·커밋 SHA 를 모두 받습니다. 셋 다
똑같이 동작하니, **항상 만들 수 있는 것**을 쓰는 게 맞습니다. `release/vX.Y.Z`
브랜치는 한 번 만든 뒤 움직이지 않으므로 소비 쪽에서는 태그와 구별되지
않습니다. 더 확실하게 하려면 커밋 SHA 를 그대로 써도 됩니다.

→ 기존 `#v1.0.0` 설치는 그대로 동작합니다. 다음에 올릴 때 `#release/v1.1.0`
  으로 바꾸세요.

### 접근성을 처음으로 재봤습니다

`npm run audit:a11y` — axe-core 로 20개 화면 × 2개 폭 × 2테마, 80건을
WCAG 2.1 A/AA 로 검사합니다. **위반 0건**입니다.

지금까지 "접근성 — 협상 불가" 라고 적어두고 한 번도 측정한 적이 없었습니다.
문서에 적힌 약속을 기계가 확인하지 않으면 그건 약속이 아닙니다. CI 가
PR 마다 돌립니다.

0건이 되기까지 고친 것들 — 전부 화면을 봐서는 못 찾는 것들입니다:

- **사이드바 배지 대비 2.66:1.** 활성 항목은 배경이 강조색이라 그 위에
  `sidebar-subtle` 을 얹으면 숫자가 거의 안 보였습니다. 활성일 때는 활성
  글자색을 씁니다
- **`role="grid"` 가 `<table>` 이 아니라 스크롤 `<div>` 에** 붙어 있었습니다.
  화면낭독기가 표를 표로 읽지 못합니다
- **`<ul>` 안에 `<ul>`** — 저장된 뷰 목록이 목록을 중첩해 만들고 있었습니다
- **이름 없는 컨트롤** — `Progress` 막대와 `Dropzone` 의 파일 입력
- **11px 글자의 대비 4.44:1** — 기준(4.5)에 아슬아슬하게 못 미쳤습니다.
  작은 글자일수록 한 단계 진한 색이 필요합니다

---

## v1.0.2

좁은 화면(390px)에서 처음으로 렌더해보고 고친 것들입니다. 지금까지 1440px
에서만 확인했는데, 정작 제출 전 확인 목록에는 "1024px 미만" 항목을 적어두고
정작 해본 적이 없었습니다.

### PageHeader 가 좁은 화면에서 깨졌습니다

제목과 액션이 항상 가로로 나란히 있어, 액션이 폭을 다 가져가면 제목 영역이
0 에 가까워졌습니다. 제목은 `운영...` 으로 잘리고 설명은 한 어절씩 세로로
쪼개져 읽을 수 없었습니다.

이제 sm 미만에서는 제목과 액션을 세로로 쌓고, 제목은 두 줄까지 허용합니다.

### Stepper 가 모바일에서 무슨 단계인지 알 수 없었습니다

`1 파... 2 열... 3 확... 4 가져...` — 네 단계를 390px 에 나란히 넣을 방법은
없습니다. 이제 좁은 화면에서는 `1 / 4 단계 · 파일 올리기` 처럼 지금 어디인지만
온전한 문장으로 보여주고, 진행은 막대로 표시합니다.

### 그 밖

- 가져오기 화면의 파일 힌트가 `.csv · 최대 20MB · 최대 20MB` 로 겹쳤습니다.
  Dropzone 이 이미 형식과 용량을 적으므로 힌트에서 뺐습니다.

→ 세 가지 모두 좁은 화면에서만 달라집니다. 데스크톱 화면은 그대로입니다.

---

## v1.0.1

버그 수정 두 건. 둘 다 **실제로 모니터링 시스템을 하나 만들어보다** 발견했습니다 —
템플릿 안에서는 드러나지 않던 것들입니다.

### 미래 날짜가 전부 "방금" 으로 나왔습니다

`formatRelative` 가 과거만 가정하고 있었습니다. 교정 만료가 9일 남았는데
"방금" 이라고 쓰면 이미 만료된 것처럼 읽힙니다. 만료일·기한·다음 실행처럼
**미래를 가리키는 날짜는 관리도구에 흔합니다.**

이제 `9일 후`, `3시간 후` 처럼 나옵니다. 일주일이 넘으면 상대 시간이 감이
안 오므로 날짜를 그대로 보여줍니다(기존 동작과 같습니다).

→ 날짜 필드를 미래 값으로 쓰던 화면이 있었다면 표시가 바뀝니다. 과거 날짜는
  그대로입니다.

### 오타 난 연산자가 필터를 조용히 풀어버렸습니다

`applyQuery` 가 모르는 연산자를 만나면 조건을 통과시켰습니다. 그러면 화면에는
"상태 = 고장" 칩이 붙어 있는데 실제로는 **전체 목록**이 나옵니다. 사용자는
그게 걸러진 결과라고 믿고 일괄 작업을 겁니다.

이제 아무것도 통과시키지 않고 콘솔에 무엇이 잘못됐는지 적습니다. 조용히
틀린 것보다 시끄럽게 비어 있는 편이 낫습니다.

→ 지금까지 이 버그 덕분에 "우연히 동작하던" 질의가 있었다면 결과가 비어
  보입니다. 콘솔에 찍힌 연산자 이름을 고치세요.
  (스키마에서 사라진 필드를 가리키는 저장된 뷰는 예전처럼 조건만 무시합니다.)

---

## v1.0.0

첫 배포. 다른 프로젝트가 설치해서 쓸 수 있는 형태가 되었습니다.

### 들어 있는 것

- **화면 원형 16종** — 대시보드·목록·대용량·알림 인박스·가져오기 마법사·
  승인 대기·예약 작업·검색 결과·사용자와 권한·감사 로그·연동과 API·
  사용량과 청구·환경설정·내 계정·로그인/인증 흐름·빈 워크스페이스·오류 4종
- **컴포넌트** — 앱 셸, DataGrid(가상화·키보드·인라인 편집), 보드 뷰,
  질의 빌더, 객체 상세, 차트, 폼, 오버레이, 상태 표시
- **데이터 모델** — 필드 스키마 하나에서 표·보드·상세·필터·질의·대시보드가
  전부 파생됩니다
- **색 팔레트 6종** — 전부 WCAG 대비 검사를 통과해야 빌드됩니다
- **Tailwind 3.x·4.x 지원** — 토큰은 한 곳(`tailwind-preset.js`)에서 나오고
  v4 용 `@theme` 은 거기서 생성됩니다

### 통일성이 강제되는 방식

- Tailwind 기본 색 팔레트를 제거했습니다. `bg-blue-500` 은 **CSS 가
  생성되지 않습니다**(v3·v4 모두).
- 간격은 4px 배수만 존재합니다. `p-7`, `h-7` 도 CSS 가 생성되지 않습니다.
- `npm run lint:design` 이 13개 규칙으로 빌드를 실패시킵니다.
- CI 가 PR마다 규칙·빌드·생성 파일 최신 여부를 확인하고, 진짜 소비
  프로젝트를 v3·v4 양쪽으로 만들어 설치·빌드까지 검증합니다.

### 알아둘 것

- Tailwind v3 에서는 `tailwind.config.js` 의 `content` 에 `hctContent` 를
  반드시 펼쳐 넣어야 합니다. v3 는 프리셋의 `content` 를 병합하지 않아,
  빼먹으면 컴포넌트 클래스가 전부 purge 되고 **오류 없이** 스타일만 빠진
  화면이 나옵니다. 빼먹으면 `AppShell` 이 콘솔에 고칠 방법을 적어줍니다.
  (v4 는 `@source` 가 패키지 CSS 안에 있어 이 문제가 없습니다.)
- 팔레트·테마 설정은 하지 않아도 됩니다. 아무 속성도 붙이지 않으면 기본
  팔레트가 그대로 적용됩니다.
- 로고 원본이 264×86 래스터입니다. 화면에는 충분하지만 인쇄·대형 확대에는
  부족합니다. SVG 가 확보되면 `Logo.jsx` 만 고치면 됩니다.
